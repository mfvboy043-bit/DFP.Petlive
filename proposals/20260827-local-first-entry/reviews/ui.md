# UI review
Verdict: pass

Candidate: worktree `.worktrees/local-first-entry` (Google-gate revise vs iteration-1 local-first). Judged A first viewport, discarded gear, and unsigned B chrome only.

GG revise clears the iteration-1 hierarchy fight: no「進入護照」, no unsigned connect chip, gear stays hidden. A reads as one composition — brand mark + eyebrow + 「火龍果護照」 + lede + tertiary demo + mascot — with the sole formal door as the top-right filled Google chip. No hero cards. Fonts stay Fraunces / Noto Serif TC / Noto Sans TC. Motion is press/hover only.

Without an in-hero primary CTA, hierarchy is still clear: the black Google control is the only high-weight action; demo stays a quiet underline. Closed from iteration 1: **UI-001** (Google no longer competes with a sage hero primary — sole door is correct), **UI-004** (no unsigned B/C connect chip to size or clutter).

## Findings
- [UI-002] [P3] Intro A lede — support copy still leads with local-stay + Drive backup, then the medical line; EN/JA wrap denser than a single door-pointing beat. With no in-hero CTA, shorten to one support sentence that names Google as the way in (keep disclaimer as the second line) so the mascot stays in the ~375px first viewport.
- [UI-003] [P3] Intro A topbar ~421–520px — `loginWithGoogle` stays labeled until 420px, so mid-width phones keep a long black chip beside an ellipsizing brand-mark. Raise the icon-only breakpoint or cap the label so brand + door share the glass bar without crowding.
